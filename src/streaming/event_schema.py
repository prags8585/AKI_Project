EVENT_SCHEMA = {
    "id_fields": ["patientunitstayid", "subject_id", "hadm_id", "stay_id"],
    "time_field": "event_time",
    "event_type_field": "event_type",
    "value_field": "value",
    "unit_field": "unit",
}
